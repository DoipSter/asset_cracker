name: asset_cracker_engine
description: >-
  The Asset Cracker paper-trading engine: model, strategies, fees, accounting and
  settlement for Kalshi's 15-minute crypto markets. Pure Dart, no Flutter. Simulation only.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.5.0

dev_dependencies:
  test: ^1.25.0
