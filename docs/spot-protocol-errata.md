# Errata to the spot trend protocol

Append-only, under section 0.1 of `docs/v3-measurement-protocol.md` applied to `docs/spot-protocol.md`: names and
syntax only, justified from migrations and source text, never from data; committed and pushed before the run.

No errata yet.
