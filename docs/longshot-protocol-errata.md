# Errata to the long-shot protocol

Append-only, under the rule of section 0.1 of `docs/v3-measurement-protocol.md` applied to `docs/longshot-protocol.md`:
names and syntax only, justified from migrations and source text, never from query output; committed and pushed
before the run it affects.

No errata yet.
